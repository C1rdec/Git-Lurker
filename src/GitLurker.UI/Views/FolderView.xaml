<UserControl x:Class="GitLurker.UI.Views.FolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitLurker.UI.Views"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Left" ShowGridLines="True" Margin="0,5,0,0">
        <TextBlock x:Name="Folder" FontSize="38"/>
        <Button VerticalAlignment="Top" HorizontalAlignment="Right" x:Name="Delete" BorderThickness="0" Cursor="Hand" Margin="0,-5,-25,0">
            <Button.ContentTemplate>
                <DataTemplate>
                    <iconPacks:PackIconMaterial Kind="Delete" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.3"/>
                </DataTemplate>
            </Button.ContentTemplate>
        </Button>
    </Grid>
</UserControl>
