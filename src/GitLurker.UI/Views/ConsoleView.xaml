<UserControl x:Class="GitLurker.UI.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:busy="https://github.com/Peoky/BusyIndicator"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="IndicatorForeground" Color="{StaticResource MahApps.Colors.Accent}" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="#383838" CornerRadius="10">
        <Grid>
            <ScrollViewer  x:Name="ScrollViewer"
                           Margin="10"
                           HorizontalScrollBarVisibility="Hidden" 
                           PreviewMouseWheel="ScrollViewer_PreviewMouseWheel" 
                           VerticalScrollBarVisibility="Hidden">
                <ItemsControl x:Name="Lines">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="16" FontFamily="Consola" Foreground="{DynamicResource Brushes.Secondary}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <busy:BusyMask x:Name="BusyIndicator" IsBusy="{Binding IsLoading}" IndicatorType="Piston" BusyContent=" " Background="Transparent" VerticalAlignment="Center" Height="80" Margin="0,0,0,-20"/>
        </Grid>
    </Border>
</UserControl>
