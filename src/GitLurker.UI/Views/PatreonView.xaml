<mah:MetroWindow x:Class="GitLurker.UI.Views.PatreonView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 WindowStartupLocation="CenterScreen"
                 mc:Ignorable="d"
                 Title="PatreonView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center" MaxWidth="300">
            <TextBlock x:Name="PatreonId" HorizontalAlignment="Center"/>
            <Button x:Name="Login" Grid.Row="1" Visibility="{Binding IsNotLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">Login</Button>
            <Grid Visibility="{Binding IsNotPledged, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button x:Name="Pledge" Grid.Row="1">BECOME A PATRON</Button>
                <Button x:Name="Refresh" Grid.Column="1">Refresh</Button>
            </Grid>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
