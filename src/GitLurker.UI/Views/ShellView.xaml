<Window x:Class="GitLurker.UI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Topmost="True"
        Background="Transparent"
        Title="ShellView" Height="450" Width="800">
    <Grid Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBox x:Name="SearchTerm"
                 Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="45" 
                 BorderThickness="0,0,0,1"
                 Margin="0,0,0,10" 
                 Controls:TextBoxHelper.SelectAllOnFocus="True"
                 Controls:TextBoxHelper.Watermark="Search"
                 BorderBrush="{DynamicResource MahApps.Brushes.Accent}"></TextBox>
        <ContentControl x:Name="WorkspaceViewModel" Grid.Row="1"/>
    </Grid>
</Window>
